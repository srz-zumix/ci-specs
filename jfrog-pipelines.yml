resources:
  - name: ci_specs_gitRepo
    type: GitRepo
    configuration:
      gitProvider: GitHub
      path: srz-zumix/ci-specs
      branches:
        include:
          - master
          - ^feature/.*
pipelines:
  - name: centos
    configuration:
      nodePool: centos-latest
    steps:
      - name: spec
        type: Bash
        configuration:
          inputResources:
            - name: ci_specs_gitRepo
        execution:
          onExecute:
            - ${res_ci_specs_gitRepo_resourcePath}/.ci/spec.sh
